{
  "name": "tfl",
  "version": "0.0.1",
  "description": "TFL API test",
  "main": "index.html",
  "scripts": {
    "start": "npm install && bower install && node server.js && echo 'Open a browser localhost:3000'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "tfl",
    "api"
  ],
  "author": "Adrián de la Rosa <adrian@delarosab.me>",
  "license": "MIT",
  "dependencies": {
    "bower": "^1.3.12",
    "connect": "^3.3.4",
    "serve-static": "^1.9.1"
  }
}
